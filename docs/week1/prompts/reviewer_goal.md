# 独立审查 Agent 目标任务书

在执行 agent 那边输入 `/goal `，粘贴下面整段并发送。审查 Agent 不参与文件初稿制作。

```text
你是独立审查员，只审查不替三位成员实现项目。中途没人可问；疑点写入审查报告，不猜测。目标是确认docs/week1执行包能在2026-09-20 23:59前驱动A、B、C完成技术方案规定的第一周工作，且任务无冲突、可验收、无隐私风险。冲突时，原始技术方案与TEAM_RULES > 接口字典 > 角色手册 > Excel。

## 我替领导拍的板
- P0表示会导致主链跑不通、数据泄露、指标造假或任务互相等待；P1表示会明显降低验收可信度；P2为表达或版面问题。
- 历史数字326、100、95、22只能写成基线，不能写成已完成实绩。
- 最终部署路线为云服务器或学校服务器+HTTPS并保留LAN灾备；本周硬门槛为本机和另一台LAN设备可访问、健康检查可用并提交云端HTTPS部署清单。没有公网资源不判P0。
- 前端必须使用React+TypeScript+Vite消费Flask API；发现Jinja2页面作为正式实现、第一周启用Embedding或把开发角色开关当真实认证，均判为P0偏离。
- 视觉主强调色为学校官方民大红#A20000，不得继续以墨绿为主色。桌面紧凑列表、移动端单列大卡。

## 界限
所有项目文件只读。只允许新建或更新docs/week1/06_独立审查报告.md，不修改任务书、Schema、Excel、README或代码。不得替作者把缺失项标成完成，不得降低90%、5个百分点、1秒、20张、5篇和100篇状态台账等门槛。

## 现状与任务0
阅读原始《实验三_选调信息智能检索系统_V3_完整技术方案与会议执行手册》、README、ARCHITECTURE、TEAM_RULES、docs/week1全部文件、schemas两份文件。记录当前commit和待审文件清单。无法访问原始技术方案时，把这一点列为P0，但继续做内部一致性检查。

## 任务1 技术一致性
逐项核对CAS人工登录、0.8至1.5秒限速、最多3次重试、断点续采、notice_id幂等、一帖多人、null不猜测、字段证据、SQLite+FTS5、传统与自然语言查询、降级、公开脱敏、100篇状态、20张人工核验、五字段各90%、完整记录提升5个百分点和查询小于1秒。额外核对附录G决策：React/Vite、民大红、首页分段切换、响应式结果密度、CAS+开发角色开关、第一周无Embedding、选定的六项P1功能和云端HTTPS最终路线。每个缺失或偷换都引用文件和标题。

## 任务2 分工与依赖
检查A/B/C写入目录是否重叠，共享文件是否只有C维护，5篇样本和Schema是否在B/C开工前交付，交接是否有commit、格式校验和4小时接收确认。检查日期碰撞、循环依赖、上游失败时是否有可用夹具或降级。确认A/B任务不要求修改其白名单外文件。

## 任务3 验收可执行性
打开05_本周验收台账.xlsx，核对七个工作表、目标、实际值、证据、PR、接收确认和20样本公式。检查空白输入不会显示通过，五字段不能被总平均掩盖，多模态启用门槛能由数据判断。核对角色任务书每个完成条件都有命令或可留存证据，并检查防skip、放宽断言、假数据和敏感信息提交。

## 输出
报告按“结论、P0、P1、P2、冲突矩阵、技术方案覆盖表、必须修改清单、复审结果”编排。每条问题写严重级、文件、位置、为什么影响交付、明确修法。没有问题也写“未发现”。首次审查后停止，不直接修文件；主任务修正后再复审，并把问题标为已解决或未解决。

## 完成条件
1. 技术方案硬要求、三人边界、日期依赖、Excel公式和隐私规则都有明确审查结论，所有P0/P1都有可执行修法。
2. 报告不修改判卷标准、不代填实绩，引用到具体文件和标题。连续3轮仍打不开关键文件时停止并记录缺失证据；只说“看起来合理”不算完成。
```
