# 角色 B 目标任务书

在执行 agent 那边输入 `/goal `，粘贴下面整段并发送。

```text
你是角色B。这份任务书是唯一任务来源；没人能在执行中答疑，拿不准的写进progress/week1/B/BLOCKED.md并继续其他工作。换会话先读progress/week1/B/PROGRESS.md。目标是在2026-09-20 15:00前交付可批处理的OCR与规则抽取模块，并用同一批20张海报完成OCR和多模态对照。冲突时，证据与人工金标准 > 契约兼容 > 准确率 > 速度。硬约束不可改，建议若调整要在角色PROGRESS.md说明。

## 我替领导拍的板
- 本周可用PaddleOCR和多模态API｜API密钥只放.env，本周完成20张同样本对照。
- 每字段准确率按20张中正确样本数/可判定样本数计算｜缺失金标准不计分，但必须报告分母。
- 五个核心字段是届别、学历、专业、城市、岗位｜每项目标至少90%，不许用整体平均掩盖弱项。
- 多模态仅在完整记录准确率比OCR提高至少5个百分点且成本可接受时进入低置信度兜底。

## 界限
只允许修改app/extraction/**、tests/extraction/**、data/gold/**、reports/extraction/**、evidence/week1/B/**和progress/week1/B/**。不得修改采集、数据库、网页、Schema、requirements或Excel验收台账。B把实际值、证据路径和交接信息写入角色PROGRESS.md或PR清单，由C回填主验收台账。原图和真实姓名不得提交；data/gold只放匿名样本ID、标准字段、证据摘要和图像哈希。不得把缺失字段猜成常见值。

## 现状与任务0
先读README、架构、团队规则、接口字典和两份Schema。运行git status --short、git log -1 --oneline、python --version，确认分支feat/week1-extraction-eval。验证PaddleOCR可导入、多模态API可做一次脱敏烟测。把目标、顺序、最大风险写入不超过10行的progress/week1/B/PROGRESS.md；A的固定样本未到时先用20样本中的匿名本地素材工作，不改接口。

## 任务1 金标准与OCR
建立固定20样本清单，人工填写届别、学历、专业、城市、岗位和完整记录，保存图像SHA-256和标注人。实现OCR输出文字、坐标、耗时和错误状态；文本清洗后按年份、学历、学院、专业、城市词典和版面顺序抽取。支持一帖多人，每人独立record_key和证据。

## 任务2 规则、复核与证据包
输出extraction_bundle.v1，并为每条记录生成可检索evidence pack，至少含record_key、notice_id、结构化字段、字段证据、来源URL和复核状态。字段缺失写null；关键字段无证据、规则冲突、低置信度或多人边界不清时标记review_required。自动化测试至少覆盖文本帖、单人海报、多人海报、缺字段、冲突字段、模糊OCR、证据缺失和重复处理。故意删除一条必填证据，证明Schema或测试变红；还原后贴全绿输出。

## 任务3 同样本评测
在同一20样本上运行OCR路线和多模态路线。分别统计五字段准确率、完整记录准确率、平均耗时、P95耗时、成功数、失败数和单张估算成本。报告每项分子分母。五字段任一低于90%必须列具体样本并进入复核队列；不得只给总平均。是否启用多模态必须明确写“启用”或“不启用”及依据。评测记录服务商、模型版本、发送字段或素材类型、授权依据、价格日期和保留策略；未经确认不得上传原始个人海报，只能使用已脱敏且获准的内部评测素材。

## 任务4 交接
9月17日18:00收到A的固定样本commit后，22:00前完成5篇固定样本抽取烟测：文本帖走html_rule，海报走OCR，混合帖保留两类证据；18日22:00前完成金标准和批处理；19日18:00前提交评测报告、extraction bundle、测试输出和Draft PR。每天至少push一次，提交用feat(extraction)或test(extraction)。C按commit导入后4小时内处理契约问题。

## 规矩
不得skip/todo、放宽正确判定、删难样本、改变20样本集合、用模型输出充当人工金标准、mock被测抽取器或修改90%/5个百分点门槛。测试和样本数只增不减。新增依赖只写PR说明。同一验收失败3次即停该项并记录。

## 完成条件
1. 20样本金标准完整，OCR与多模态都产生可复核结果，五字段逐项指标、完整记录指标、耗时和成本均可从原始结果重算。
2. 5篇固定样本输出全部通过extraction_bundle.v1校验且都有可检索evidence pack，低置信度不静默发布，git diff不越界且敏感原图为0。progress/week1/B/BLOCKED.md必须提交，没问题也写“无”；只贴“完成”不算，必须附实际命令和红到绿证据，或连续3轮受外部模型阻塞后如实交卷。
```
