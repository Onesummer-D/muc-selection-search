# 第二周独立审查报告（只读）

审查范围：`feat/week2-integration`、B 允许目录、C 用户功能接口、第一周台账和证据索引。审查 Agent 不修改实现。

## 结论

当前可以确认的代码交付：B 的 extraction/gold/reports 已按白名单迁移；C 已加入 SQLite 用户表、保存搜索、隐私、历史、提醒和无痕服务端入口；管理员发布/撤回路由写入审计事件；A 已冻结 AuthProvider/CAS 契约与备份工具。

仍不能宣称已闭环的 P0：第一周台账四元组（实际值、证据路径、Commit/PR、接收确认）仍有空项；LAN 第二设备截图/录屏尚待人工补交；B 的固定 5 篇 bundle 尚需在 C SQLite 中完成导入烟测；真实 CAS、公网 HTTPS 和全量 100 篇生产快照依赖外部环境；游客派生图的实际 Network 证据尚需复测。

## 逐项检查

1. A/B/C 分支边界：B 只允许 `app/extraction`、`tests/extraction`、`data/gold`、`reports/extraction`、`progress/week1/B`、`evidence/week1/B`；根配置、领域层、仓储层、搜索层、前端和文档必须人工审查。
2. 评测口径：本周验收以归一化准确率为主判定，仍固定同一 20 样本、同一 SHA-256、同一 gold。原始逐样本结果和严格口径报告只作为可追溯附件，不得删除或替换样本。多模态是否启用仍须按既定差值门槛判断，未达标保持“不启用”。
3. 用户持久化：接口按 user_id 隔离；历史/推荐默认关闭；打开后保留 90 天；删除历史后不再参与画像；无痕请求不写个人持久化表。
4. 游客安全：`RecordPresenter` 仍需通过 Network 响应复测，不能只看 DOM/CSS；不得出现姓名、原图引用、联系方式、二维码、会议入口或受限来源 URL。
5. 日期与交接：9 月 26 日 18:00 P0 冻结，9 月 27 日只修缺陷、录屏、台账和审查材料；每次交接至少预留 4 小时。

## 阻断项

若验收表任一实际值、证据、Commit/PR、接收确认为空，状态只能是“待填写/缺证据/待接收”，不能显示“通过”。
