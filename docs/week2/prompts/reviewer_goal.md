# Reviewer Goal：第二周独立审查

只读检查，不修改实现。检查第一周 P0/P1 是否逐项关闭，A/B/C 是否只改自己的目录，B 是否删除或覆盖 C 文件，样本/gold/严格口径是否被偷换，用户功能是否服务端持久化并按 user_id 隔离，无痕是否零写入，游客 Network 是否无私有字段，日期/四小时交接窗口/验收公式是否可执行，演示是否覆盖主链、降级、LAN 恢复和“不启用多模态”结论。

输出证据路径、命令、实际值和阻断项。验收表四元组缺一不可通过；不把代码存在当成现场证据。
