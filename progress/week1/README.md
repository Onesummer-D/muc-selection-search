# 第一周角色进度区

每名成员只修改自己的目录，使用 `PROGRESS.md` 记录目标、实际指标、命令输出摘要、证据路径、交接 commit 与接收结果，使用 `BLOCKED.md` 记录阻塞和已经尝试的方法。

`docs/week1/05_本周验收台账.xlsx` 为二进制主台账，只由角色 C 提交。A、B不直接修改该文件，C每天至少在 12:00 和 22:00 两次把各角色进度文件或 PR 清单中的数据回填到 Excel。
