# A 角色 LAN 验证收件记录

## 已收证据

| 文件 | 画面 | 结论 |
|---|---|---|
| `lan-home.png` | Chrome 打开 `http://192.168.43.50:5000` 首页 | LAN 首页可访问 |
| `lan-search.png` | 同一地址的传统搜索结果页，显示“四川 / 软件工程”查询结果 | 传统搜索链路可访问 |
| `lan-healthz.png` | `http://192.168.43.50:5000/healthz` | 返回 `status: ok`，`database: ok`，`search: ok`，`embedding: disabled_by_decision` |

## 当前边界

截图没有显示访问设备型号、访问时间或录屏信息，也没有展示详情页。因此本记录把三张截图登记为已收到的 LAN 证据，但不把完整的“另一设备首页/healthz/搜索/详情”验收项提前标记为通过。A 需要在验收台账中补填设备与时间，并补交详情页截图或录屏、Commit/PR 和 C 的接收确认。

## 证据来源

三张图片由用户提供，原始截图文件名已在本目录按功能重命名归档。截图属于验收证据，不是新的实现要求。
