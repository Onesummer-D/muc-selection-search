# A 证据目录

已收到并归档三张 LAN 访问截图：

- `lan-home.png`：`192.168.43.50:5000` 首页
- `lan-search.png`：同一地址的传统搜索结果页
- `lan-healthz.png`：`/healthz` 返回 `status: ok`，数据库与搜索组件为 `ok`

这三张图证明了首页、传统查询和健康检查链路已经从 LAN 地址打开。截图本身没有显示访问设备型号或网络时间，也没有包含详情页，因此 A 仍需补填访问时间/设备信息，并补交详情页截图或录屏，才能关闭完整的四元组验收项。备份恢复日志和真实 HTTPS/CAS 仍按条件式要求提交。没有服务器、域名或 CAS 回调时只提交 `progress/week2/A/deployment-blocker.md`，不得填入虚构 URL。
